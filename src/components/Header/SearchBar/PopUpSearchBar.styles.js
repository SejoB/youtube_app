import styled from 'styled-components'

import { HWrapper } from '../../Header/MainHeader/MainHeader.styles'

export const Container = styled(HWrapper)`
      top:0px;
      left: 0px;
      position: absolute;
      /* z-index: 30px; */
      /* display: none; */
      /* display: ${props=>props.showSBar ? 'display:flex' : 'none'} */
`

