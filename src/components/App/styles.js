import styled from 'styled-components'

import { blackColor }  from '../../styled/constants'


export const AppWrapper = styled.div`
      display: flex;
      width: 100%;
      height: 100%;
      background-color: ${ blackColor };
      font-family: 'Roboto', Arial, sans-serif;
`